export type TPost = {
  id: number;
  title: string;
  description: string;
  image: string;
  cta: string;
};

export type TitleProps = {
  subHeading: string;
  heading: string;
  generalText: string;
};
